# Copy terraform_state outputs here; initialise with -backend-config=backend.hcl.
bucket         = "morning-brief-tf-state-CHANGEME"
key            = "morning-brief/terraform.tfstate"
region         = "eu-west-2"
use_lockfile   = true
encrypt        = true
