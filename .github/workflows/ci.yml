name: CI

# Runs the shared quality gate (gate.yml) on every PR and every push to main.
# The deploy workflow calls the same gate in front of each roll.

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  gate:
    uses: ./.github/workflows/gate.yml
